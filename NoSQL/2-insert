// Insert a document into the 'school' collection
const existingSchool = db.school.findOne({ name: "Holberton school" });

if (!existingSchool) {
  db.school.insertOne({
    name: "Holberton school",
  });
}
